document.addEventListener("DOMContentLoaded",()=>{const header=document.getElementById("header"),menu=document.getElementById("menu"),toggle=document.getElementById("menuToggle"),toast=document.getElementById("toast");document.getElementById("year").textContent=new Date().getFullYear();const setHeader=()=>header.classList.toggle("scrolled",scrollY>30);setHeader();addEventListener("scroll",setHeader,{passive:true});toggle.addEventListener("click",()=>{const open=menu.classList.toggle("open");document.body.classList.toggle("menu-open",open);toggle.setAttribute("aria-expanded",String(open));toggle.setAttribute("aria-label",open?"Cerrar menú":"Abrir menú")});menu.querySelectorAll("a").forEach(a=>a.addEventListener("click",()=>{menu.classList.remove("open");document.body.classList.remove("menu-open");toggle.setAttribute("aria-expanded","false")}));let timer;const notify=message=>{clearTimeout(timer);toast.textContent=message;toast.classList.add("visible");timer=setTimeout(()=>toast.classList.remove("visible"),4200)};document.querySelectorAll("[data-pending]").forEach(button=>button.addEventListener("click",()=>notify(button.dataset.pending)));const eventDate=new Date("2026-10-29T09:00:00-06:00").getTime(),write=(id,value)=>document.getElementById(id).textContent=String(value).padStart(2,"0"),count=()=>{const diff=eventDate-Date.now();if(diff<=0){["days","hours","minutes","seconds"].forEach(id=>write(id,0));return}write("days",Math.floor(diff/86400000));write("hours",Math.floor(diff%86400000/3600000));write("minutes",Math.floor(diff%3600000/60000));write("seconds",Math.floor(diff%60000/1000))};count();setInterval(count,1000);const reveals=document.querySelectorAll(".reveal");if("IntersectionObserver"in window){const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("visible");observer.unobserve(entry.target)}}),{threshold:.12});reveals.forEach(el=>observer.observe(el))}else reveals.forEach(el=>el.classList.add("visible"))});